package com.teleclaw

import android.os.Bundle
import androidx.appcompat.app.AppCompatActivity

/**
 * Main entry point for the TeleClaw UI.
 *
 * UI rendering and navigation are handled by proprietary modules
 * not included in this open framework repository.
 */
class MainActivity : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        // UI rendering handled by proprietary module
    }
}
